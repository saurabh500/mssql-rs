# tdsapp project
tdsapp project produces an executable binary that used a prototype of tdslibrary project `..\tdslibrary`.

The application connects to the SQL server by sending a TDS prelogin and login packets.
