[package]
name = "connect-sync-app"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
connect-sync-lib = { path = "../connect-sync-lib" }
tracing = "0.1"
tracing-subscriber = "0.3.0"
