# connect-sync-app project
connect-sync-app project produces an executable binary that used a prototype of connect-sync-lib project `..\connect-sync-lib`.

The application connects to the SQL server by sending a TDS prelogin and login packets.
The implementation uses synchronous I/O operations.
