[package]
name = "transport-app"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
transport-lib = { path = "../transport-lib" }
tracing = "0.1.40"
tracing-subscriber = "0.3.18"
