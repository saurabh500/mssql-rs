# connect-async-app project
connect-async-app project produces an executable binary that used a prototype of connect-async-lib project `..\connect-async-lib`.

The application connects to the SQL server by sending a TDS prelogin and login packets.
The implementation uses asynchronous I/O operations.
