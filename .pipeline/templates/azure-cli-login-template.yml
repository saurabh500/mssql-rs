steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'ACR-Tdslibrs'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'az acr login -n tdslibrs'
  retryCountOnTaskFailure: 5