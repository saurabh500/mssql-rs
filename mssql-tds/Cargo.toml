[package]
name = "mssql-tds-preview"
version = "0.1.0-preview.5"
edition = "2024"
description = "Rust implementation of the TDS (Tabular Data Stream) protocol for SQL Server. Publishable fork of microsoft/mssql-rs until the official crate lands on crates.io."
license = "MIT"
readme = "README.md"
repository = "https://github.com/saurabh500/mssql-rs"
keywords = ["sql-server", "tds", "mssql", "database", "async"]
categories = ["database", "network-programming"]

[lib]
name = "mssql_tds"

[features]
default = ["integrated-auth", "native-tls-backend"]
# Windows SSPI (Security Support Provider Interface) for integrated authentication
sspi = []
# Unix GSSAPI (Kerberos) for integrated authentication
gssapi = []
# Enable both SSPI and GSSAPI for cross-platform integrated authentication
integrated-auth = ["sspi", "gssapi"]
# TLS backend selection (exactly one must be enabled)
native-tls-backend = ["dep:native-tls", "dep:tokio-native-tls"]
rustls-backend = ["dep:tokio-rustls", "dep:rustls", "dep:rustls-pki-types", "dep:rustls-native-certs", "dep:webpki-roots"]


[lints]
workspace = true

[dependencies]
async-trait = "0.1.89"
tokio = { version = "1.48.0", features = ["full"] }
tokio-native-tls = { version = "0.3.1", optional = true }
native-tls = { version = "0.2.18", features = ["alpn"], optional = true }
# rustls TLS backend (optional)
tokio-rustls = { version = "0.26.4", optional = true }
rustls = { version = "0.23.40", default-features = false, features = ["std", "tls12", "ring"], optional = true }
rustls-pki-types = { version = "1.9", optional = true }
rustls-native-certs = { version = "0.8.3", optional = true }
webpki-roots = { version = "1.0.7", optional = true }
futures = "0.3.31"
byteorder = "1.5.0"
tokio-util = { version = "0.7.17", features = ["full"] }
bytes = "1.11.0"
tracing = { version = "0.1.43", features = ["log"] }
bitflags = "2.10.0"
thiserror = "2.0.17"
bigdecimal = "0.4"
# For pretty printing of the packets in the traces.
pretty-hex = "0.4"
# Added for setting the keep alives on the socket.
socket2 = { version = "0.5", features = ["all"] }
hostname = "0.4.2"
# For LCID-based encoding conversion (TEXT/NTEXT reading)
encoding_rs = "0.8"
# For certificate expiry validation (ServerCertificate feature)
x509-parser = "0.18.0"
# For DNS resolution (reverse DNS lookup for SPN canonicalization)
dns-lookup = "2.0"
# Required to deserialize Vector(Float16) values from the TDS stream (Vector feature v2)
half = "2.7.1"



[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["namedpipeapi", "winerror", "winbase"] }
windows = { version = "0.62", features = ["Win32_Foundation", "Win32_System_LibraryLoader", "Win32_System_Registry", "Win32_System_SystemInformation", "Wdk_System_SystemServices"] }

[target.'cfg(unix)'.dependencies]
# For runtime GSSAPI library detection using dlopen/dlsym
libc = "0.2"

[dependencies.uuid]
version = "1.19.0"
features = [
    "v4",                # Lets you generate random UUIDs
    "fast-rng",          # Use a faster (but still sufficiently random) RNG
    "macro-diagnostics", # Enable better diagnostics for compile-time UUIDs
]

[[bench]]
name = "perf"
harness = false

[dev-dependencies]
rand = "0.9.2"
dotenv = "0.15"
azure_identity = "0.25.0"
azure_core = "0.25.0"
ctor = "0.4"
mockall = "0.13.1"
criterion = "0.5"
mssql-mock-tds = { path = "../mssql-mock-tds", default-features = false }
tempfile = "3.13"
tracing-subscriber = { version = "0.3.22", features = ["env-filter", "fmt"] }
